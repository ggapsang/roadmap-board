# WOLFPACK — 개발 규약

일정을 시간축 위에서 편집·공유하는 데스크톱 보드. 마스코트는 FENRIR.
기획안은 `refs/roadmap-board_기획안.md`, P0 단일 파일 시안은 `refs/index.html`,
디자인 세부 규격은 `refs/WOLFPACK_{Light,Dark}_UI_Design_Guide.md`에 있다.
**refs/ 는 읽기 전용 참고 자료다. 수정하지 않는다.**

## 실행

```
npm install
npm start          # 앱 실행
npm run dev        # 개발자 도구 함께
npm test           # 스모크 — 창을 띄워 렌더/저장 왕복을 확인하고 종료
npm test -- --shot ./shots   # 스모크에 화면 캡처 추가 (라이트/다크 둘 다)
npm start -- --db ./other.db   # DB 파일 지정
```

## 구조

```
electron/          메인 프로세스 — 창, IPC, SQLite
  main.js            app:// 프로토콜, IPC 핸들러, 메뉴, 스모크
  preload.cjs        contextBridge로 window.roadmapDB만 노출 (sandbox:true라 CJS)
  db/index.js        연결 + PRAGMA user_version 기반 마이그레이션
  db/repository.js   문서 <-> 정규화 테이블
  db/migrations/     NNN_*.sql — 번호 순 적용

src/               렌더러 (프레임워크 없음, ES 모듈)
  config/            상태·조직·배율 등 런타임 설정, 기본 로드맵(seed)
  core/              DOM을 모르는 순수 로직
    arrow-geometry.js  직교 경로 -> 블록 화살표 다각형 (순수 계산)
    dates.js           날짜 (로컬 타임존 고정)
    schema.js          문서 버전 · 마이그레이션 · 정규화
    store.js           문서 상태 + 되돌리기 + 저장
    view.js            화면 상태 (필터/검색/배율/선택) — 저장도 undo도 안 됨
    layout.js          레인(겹침) 배치 — 순수 함수. 트랙/상위 일정 안에서 같은 규칙
    storage.js         저장소 어댑터 (Local / Electron / Memory)
  ui/                DOM 렌더
    launcher.js        프로젝트 목록 — 앱의 첫 화면
    board/             축 · 헤더 · 카드 · 화살표 · 드래그
    panels/            일정 편집 · 보드 구성(트랙+조직) · 데이터
```

## 지켜야 할 것

1. **문서 상태와 화면 상태를 섞지 않는다.** 저장·공유되는 것만 `Store`에.
   필터·검색어·배율·선택은 `ViewState`. 되돌리기가 화면을 흔들면 안 된다.
2. **변경은 전부 `store.commit()` / `store.begin()~end()`를 통과한다.**
   스냅샷·저장·이벤트 발행이 그 한 곳에만 있다.
3. **날짜는 로컬 타임존 `Date` + 일 단위 정수.** UTC 혼용 금지 (하루 밀림).
   종료일은 inclusive.
4. **디자인은 WOLFPACK 가이드를 따른다.** 계층은 Surface 깊이·여백·약한 Border로
   만들고, Orange는 오늘·선택·진행 표시자·CTA·포커스에만 쓴다. 카드 배경을 상태
   색으로 칠하지 않는다(좌측 4px indicator). 카드에 그림자를 넣지 않는다 —
   그림자는 Dropdown·Dialog 같은 "떠 있는 계층"에만.
5. **사용자가 정하는 것을 코드에 박지 않는다.** 트랙 구성과 담당 조직 목록은
   문서 안에 있고 보드에서 편집한다. config의 `DEFAULT_ORGS`는 새 프로젝트
   초기값일 뿐이다. 트랙 축에 의미(요구사항/WP 등)를 강제하지 않는다.
6. **조직명을 바꾸면 참조도 바꾼다.** 트랙은 id 참조지만 조직은 `item.og`에
   문자열 값으로 들어간다 (반출 JSON 가독성을 위한 선택). 이름 변경·삭제 시
   해당 일정의 `og`를 함께 갱신해야 한다 — `ConfigPanel`이 그 일을 한다.
7. **색상은 시맨틱 토큰으로만.** `--accent`, `--text-secondary` 등.
   HEX 직접 사용 금지. 트랙별 임의 색상 금지 — 색은 상태를 뜻한다.
8. **간격·크기는 `--u1`~`--u10`에서만 고른다.** 4px 그리드.
9. **스키마를 바꾸면** `src/core/schema.js`의 `SCHEMA_VERSION`을 올리고
   `MIGRATIONS`에 함수를 추가한다. DB 스키마는 `migrations/`에 파일을 **추가**한다
   (기존 파일 수정 금지).
10. **화살표는 렌더 마지막에.** DOM 좌표를 읽으므로 컬럼 폭이 확정된 뒤여야 한다.
   `.col`에 z-index를 주지 말 것 — 쌓임 맥락이 생겨 컬럼 전체가 한 층으로 묶이고,
   카드의 z-index가 의도대로 먹지 않는다.
   `.ev.container`에도 z-index를 주지 말 것 — 주면 쌓임 맥락이 생겨 안에 든 카드가
   그 층에 갇히고, 화살표가 자식 카드 위로 올라와 글씨를 덮는다. z-index:auto면
   컨테이너 배경은 화살표 아래, 자식 카드는 화살표 위에 놓인다.
   화살표는 **카드 아래(z:5)** 에 그린다. `routeBetween`이 다른 카드를 피해 경로를
   고르지만 완전히 피할 수 없는 경우가 있고, 그때 위에 있으면 글씨를 덮는다.
   지나가는 카드가 화살표를 가리게 두는 편이 낫다.
   상위 일정(컨테이너)은 배경을 비워 둔다 — 칠하면 안에 든 자식으로 들어가는
   화살표가 가려진다. 장애물 목록에서도 제외한다.
11. 저장소를 늘릴 때는 `StorageAdapter`를 구현하고 `createAdapter()`에 끼운다.
   그 아래 코드는 저장 위치를 몰라야 한다.
12. **프로젝트 전환은 `store.adopt()`로.** Store/Board를 새로 만들지 않고 문서만
   갈아끼운다. `adopt`는 되돌리기 스택을 비운다 — 프로젝트 A에서 Ctrl+Z를 눌러
   B의 내용이 나오면 안 된다.
13. **마일스톤 레인 규칙이 위치에 따라 다르다.** 최상위 마일스톤은 레인 계산에서
   빼고 트랙 폭을 가로지른다(기획안 §4). 상위 일정 안에 든 마일스톤은 반대로
   형제와 레인을 나눈다 — 좁은 상자에서 폭을 다 먹으면 같이 든 막대를 덮는다.
14. **드래그 리스너를 재렌더되는 요소에 붙이지 말 것.** 드래그 중 `store.commit`이
   재렌더를 부르고, 헤더·카드는 통째로 다시 그려지면서 손잡이가 DOM에서 사라진다.
   리스너와 포인터 캡처가 같이 사라져 첫 픽셀 이후 이벤트가 끊긴다.
   `window`에 붙이고 드래그가 끝날 때 떼어 낸다 (`Board#trackResizer` 참고).
15. **트랙 구성이 바뀌면 본문 컬럼도 다시 만들어야 한다.** `render()`는 헤더만
   갱신하므로 트랙을 추가하면 그 트랙에 `.col`이 없어 카드가 들어가지 못한다.
   `Board#render`가 컬럼과 트랙 목록을 대조해 어긋나면 스스로 골격을 다시 만든다.
16. **여러 트랙에 걸치는 카드는 퍼센트로 잡을 수 없다.** `sp × 100 / lanes`로
   계산하면 레인이 2개인 트랙에서 sp=2가 100%가 되어 걸침이 상쇄되고, 트랙마다
   너비가 다르면 200%가 실제 두 칸과도 다르다. 실제 컬럼 너비를 재서 px로 잡는다
   (`Board#spanBox`). 그래서 창 크기가 바뀌면 다시 그려야 한다.
17. **가로 폭을 정하는 수단이 위치에 따라 다르다.** 최상위 카드는 트랙 걸침(`sp`)으로
   정한다 — 오른쪽 가장자리를 끌면 트랙 칸 단위로 붙는다. 상위 일정 안에 든 카드는
   걸침이 의미가 없으므로 상위 카드에 대한 비율(`x`, `w`)을 좌·우 가장자리로 잡는다.
   둘을 섞지 말 것.
18. **보드에 관한 모든 것은 `board_id`로 갈린다.** 새 테이블을 만들 때도
   `board_id`를 넣고 `ON DELETE CASCADE`를 건다.

## 데이터

`src/config/seed.js`의 기본 로드맵은 실제 과제 일정이다. 임의로 바꾸지 않는다.
