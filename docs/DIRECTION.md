# WOLFPACK — 개발 방향 점검: 순서 기반 이벤트 전개 시스템 정합성

WOLFPACK은 **순서 기반 이벤트 전개 시스템**의 첫 활용 예다.
(근거 문서: `refs/TTB-만들어진 배경-*.pdf`, `refs/TTB-순서 기반 이벤트 전개 시스템 - 개발자를 위한 설명-*.pdf`)

지금의 로드맵 보드는 그 일반형에 **"달력 눈금을 쓰고, 트랙은 과제이며, 관계는 선행이고,
보드는 하나"** 라는 설정값을 박은 상태다. 이 문서는 그 관점에서 현재 코드가 어긋나거나
앞으로의 확장을 막는 지점을 정리한 목록이다. 구현 계획이 아니라 **지켜야 할 방향**과,
지금 의식해 두면 나중이 쉬워지는 우선순위다.

> **판단 기준 (설명 문서 §6·§8)**
> *이것은 이 보드에서만 성립하는가, 아니면 이벤트 자체에 성립하는가?*
> 보드에서만 성립 → **배치·표시**. 이벤트 자체에 성립 → **이벤트·관계**.
> 애매하면 **표시 쪽**에 둔다. 표시를 나중에 본질로 올리는 건 쉽지만, 반대는 데이터를 쪼개는 일이다.

---

## 5요소 대응 (시스템 개념 ↔ 현재 코드)

| 시스템 개념 | 현재 WOLFPACK | 위치 |
|---|---|---|
| **이벤트** — 앞뒤 관계를 가진 단위. 접으면 카드, 펼치면 보드 | `item` | `src/core/schema.js`, `db/repository.js` |
| **트랙** — 이벤트가 흐르는 갈래 | `tracks[]` | `schema.js`, `ui/board/head.js` |
| **관계** — 선행·합류·원인·참조. 보드를 넘나드는 독립 대상 | `item.dp[]` (선행만, 카드의 속성) | `item.dp`, DB `dependency` |
| **순서/시간** — 앞뒤 관계. 달력은 선택적 눈금 | 항상 달력축, `dayIndex`로 위치 계산 | `core/timescale.js`, `core/layout.js` |
| **태스크** — 순서 없이 카드에 담기는 할 일 | 없음 (중첩은 하위 카드만) | — |

## 이미 지켜 온 것 (정합)

- **문서 상태(Store) ↔ 화면 상태(ViewState) 분리** — 저장·되돌리기 경계.
  단, ViewState는 세션 뷰(필터·검색·배율·선택)이지 "보드별 배치"는 아니다(→ 아래 1번).
- **사용자가 정하는 것을 코드에 안 박음** — 트랙 구성·담당 조직은 문서 안에 있고 보드에서 편집.
- **색은 상태 시맨틱** — 트랙별 임의 색 없음.
- **마일스톤 = 시점 이벤트 / 기간 마일스톤 = 기간 이벤트** — 같은 단위, 배치만 다름(최근 반영).
- **중첩(container/child)** — "이벤트가 자식을 가질 수 있는 자리"의 절반(하위 카드).

---

## 수정·정비 목록

우선순위는 **"나중에 추가하기 어려운 것 먼저"** 순이다. 지금 당장 큰 리팩터가 필요한 건 아니지만,
새 기능·필드·관계를 넣을 때마다 아래를 막지 않는지 확인한다.

### 1. 본질(이벤트)과 배치(표시)를 나눠 둔다 — 최우선
- **근거**: 설명 §3.5·§6.1·§6.5 ("나중에 추가하기 가장 어려운 부분").
- **현재**: `item` 한 객체에 본질(`ti, st, og, s, e, pg, note`)과 배치(`t, sp, x, w, hd, align, showNote`)가 섞여 있다. 보드가 하나라 지금은 문제없다.
- **어긋남**: 같은 이벤트가 두 번째 보드에 올라가는 순간, 배치가 이벤트에 붙어 있으면 전체를 쪼개야 한다.
- **방향**: 이벤트 필드(모든 보드에서 같아야 함) vs 배치 필드(보드마다 다를 수 있음)의 경계를 스키마에 명시. 새 필드는 §6 기준으로 어느 쪽인지 정하고, 배치성 필드는 향후 `(board_id, event_id)` 배치 테이블로 옮길 후보로 둔다.
- **지금 할 최소 조치**: `schema.js`의 `item` 필드에 본질/배치 경계 주석. README 스키마 예시에도 구분 표기.

### 2. 관계를 이벤트와 동등한 일급 객체로
- **근거**: §3.6·§6.3·§6.7.
- **현재**: `item.dp: string[]` — 선행 하나를 카드의 속성으로 저장. DB `dependency(board_id, item_id, depends_on)`로 정규화는 돼 있으나 문서 모델에서는 item 속성이고 종류가 없다.
- **어긋남**: 보드를 넘나드는 관계, 관계의 **종류**(합류·원인·참조·포함), 종류별 규칙을 표현할 수 없다.
- **방향**: `relations: [{id, type, from, to}]` 컬렉션(문서·DB 모두)으로 승격. `type` 목록은 **설정으로 확장**(코드에 박지 않음). 선행은 "relations의 한 종류"가 된다.
- **지금 할 최소 조치**: `dp`를 유지하되 "선행 = 관계의 한 종류"로 재해석 가능하게 주석. 관계 종류 상수를 `config`로 뺄 자리 마련.

### 3. 이벤트에 보드-독립 식별자
- **근거**: §6.2.
- **현재**: `item.id`는 충분히 고유하나 DB에서 `(board_id, id)`로 스코프되고, 이벤트는 한 보드에만 존재한다.
- **어긋남**: 같은 이벤트가 여러 보드에 나타나려면 보드에 의존하지 않는 유일 식별이 필요하다. `ord`(정렬 순번)처럼 보드-의존 식별을 관계 키로 쓰면 보드를 넘는 순간 충돌한다.
- **방향**: 식별을 보드에서 떼어 낸다. 관계·배치는 이 식별자를 참조.

### 4. 순서를 1차로, 달력을 선택적 눈금으로
- **근거**: §3.1·§6.4·§8.
- **현재**: 축은 항상 달력. `TimeScale`·`layout`이 `s·e`(날짜)와 `dayIndex`로만 위치를 계산해 **날짜 없는 보드가 불가능**하다. 축 방향(세로)도 고정.
- **어긋남**: "무엇이 무엇보다 먼저인가"만 중요한 프로세스 개념도를 못 그린다. 축 방향·눈금 종류는 데이터가 아니라 표시 선택이어야 한다.
- **방향**: **"순서상 위치"를 먼저 구하고**, 달력 눈금이 켜졌을 때만 날짜로 환산하는 스케일 계층. 축 방향(세로/가로)·눈금 종류(달력/순서)를 `meta.display` 설정으로.
- **지금 할 최소 조치**: `TimeScale`을 "달력 스케일"의 한 구현으로 이름·주석 정리하고, `meta.display`에 축·눈금 자리를 남겨 둔다.

### 5. 관계 종류별 구조 규칙 검증을 한 곳에
- **근거**: §6.7.
- **현재**: 선행(`dp`)의 순환 검증이 한 곳에 모여 있지 않다.
- **방향**: 관계 저장·검증 지점을 한 곳으로 모아 종류별 규칙을 적용 — 선행·원인 = **순환 금지(DAG)**, 합류 = **트리(합류 대상 하나)**, 참조 = **순환 허용**, 포함 = **다중부모 DAG(하위 카드끼리는 순서)**. 종류가 늘어도 규칙만 추가한다.

### 6. 태스크(순서 없는 포함)의 자리를 남긴다
- **근거**: §3.3·§6.6.
- **현재**: 중첩은 `parent`로 "하위 카드(순서 있는 포함)"만. 카드 안에 순서 없는 할 일(액션 아이템) 목록이 없다(`note`가 약한 대체).
- **방향**: 카드가 **순서 없는 태스크**를 담게 한다. 태스크끼리 앞뒤가 생기면 **하위 카드로 승격**(같은 단위, "순서축 위에 놓이는가"만 다름).

### 7. 카드↔보드 펼침·드릴다운 재귀 여지
- **근거**: §6.6·§8.
- **현재**: 중첩 컨테이너는 있으나 "카드를 펼쳐 하나의 보드로", "트랙을 펼쳐 그 안 흐름을", 드릴다운/브레드크럼은 없다.
- **방향**: 지금 구현 안 해도 되나, **한 문법(순서축·트랙·관계선)이 모든 층위에 같게 적용**되는 전제를 깨지 않는다.

### 8. 지금은 "막지만 않으면" 되는 것
다중 보드 전환, 보드 간 영향 전파(선행 지연이 보드를 넘어 퍼지기), 보드별 공유 범위(별칭·표시 방식), 축 방향 전환.
지금 내리는 구조적 결정이 이것들을 **불가능하게 만들지 않도록** 한다.

---

## 요약

- 로드맵 보드 설정값으로는 지금 코드가 잘 돈다. 즉시 갈아엎을 필요는 없다.
- 다만 새 기능·필드·관계를 넣을 때마다 §6 판단 기준을 적용하고, 특히 **1(본질/배치 분리)·2(관계 일급화)** 를 막는 결정을 피한다.
- `refs/TTB-*.pdf` 두 문서를 이 저장소의 **설계 언어**로 삼는다. 코드 규약은 [CLAUDE.md](../CLAUDE.md).
